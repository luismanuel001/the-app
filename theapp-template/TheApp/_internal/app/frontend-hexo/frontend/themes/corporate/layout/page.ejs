<div class="container">
  <ul class="breadcrumb">
    <li><a href="/">Home</a></li>
    <% if (page.title !== 'Home') { %>
    <li class="active"><%- page.title %></li>
    <% }%>
  </ul>
  <div class="main">
    <% if (page.title !== 'Home') { %>
    <h1><%- page.title %></h1>
    <% }%>
    <%- page.content %>
  </div>
</div>
